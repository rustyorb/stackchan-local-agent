diff --git a/main/xiaozhi-server/config/config_loader.py b/main/xiaozhi-server/config/config_loader.py
index 9e7e1c1..48b680e 100644
--- a/main/xiaozhi-server/config/config_loader.py
+++ b/main/xiaozhi-server/config/config_loader.py
@@ -35,9 +35,10 @@ async def load_config():
     default_config_path = get_project_dir() + "config.yaml"
     custom_config_path = get_project_dir() + "data/.config.yaml"
 
-    # 加载默认配置
-    default_config = read_config(default_config_path)
+    # A standalone local config deliberately does not inherit provider,
+    # plugin, or cloud defaults from config.yaml.
     custom_config = read_config(custom_config_path)
+    default_config = {} if custom_config.get("standalone_config") else read_config(default_config_path)
 
     if custom_config.get("manager-api", {}).get("url"):
         config = await get_config_from_api_async(custom_config)
diff --git a/main/xiaozhi-server/core/connection.py b/main/xiaozhi-server/core/connection.py
index 15ac136..0feaa0e 100644
--- a/main/xiaozhi-server/core/connection.py
+++ b/main/xiaozhi-server/core/connection.py
@@ -181,9 +181,10 @@ class ConnectionHandler:
         self.load_function_plugin = False
         self.intent_type = "nointent"
 
+        idle_value = self.config.get("close_connection_no_voice_time", 120)
         self.timeout_seconds = (
-                int(self.config.get("close_connection_no_voice_time", 120)) + 60
-        )  # 在原来第一道关闭的基础上加60秒，进行二道关闭
+            int(idle_value) + 60 if idle_value is not None and int(idle_value) > 0 else None
+        )  # None/0 means Never; transport/provider timeouts remain separate
         self.timeout_task = None
 
         # {"mcp":true} 表示启用MCP功能
@@ -1726,7 +1727,7 @@ class ConnectionHandler:
                     last_activity_time = self.first_activity_time
 
                 # 检查是否超时（只有在时间戳已初始化的情况下）
-                if last_activity_time > 0.0:
+                if self.timeout_seconds is not None and last_activity_time > 0.0:
                     current_time = time.time() * 1000
                     if current_time - last_activity_time > self.timeout_seconds * 1000:
                         if not self.stop_event.is_set():
diff --git a/main/xiaozhi-server/core/handle/receiveAudioHandle.py b/main/xiaozhi-server/core/handle/receiveAudioHandle.py
index 504669b..b9d67d2 100644
--- a/main/xiaozhi-server/core/handle/receiveAudioHandle.py
+++ b/main/xiaozhi-server/core/handle/receiveAudioHandle.py
@@ -110,11 +110,13 @@ async def no_voice_close_connect(conn: "ConnectionHandler", have_voice):
     # 只有在已经初始化过时间戳的情况下才进行超时检查
     if conn.last_activity_time > 0.0:
         no_voice_time = time.time() * 1000 - conn.last_activity_time
-        close_connection_no_voice_time = int(
-            conn.config.get("close_connection_no_voice_time", 120)
+        idle_value = conn.config.get("close_connection_no_voice_time", 120)
+        close_connection_no_voice_time = (
+            int(idle_value) if idle_value is not None else 0
         )
         if (
-            not conn.close_after_chat
+            close_connection_no_voice_time > 0
+            and not conn.close_after_chat
             and no_voice_time > 1000 * close_connection_no_voice_time
         ):
             conn.close_after_chat = True
