diff --git a/firmware/main/stackchan/avatar/skins/default/default.h b/firmware/main/stackchan/avatar/skins/default/default.h
index e4d3400..3d9f004 100644
--- a/firmware/main/stackchan/avatar/skins/default/default.h
+++ b/firmware/main/stackchan/avatar/skins/default/default.h
@@ -46,11 +46,15 @@ public:
 
 private:
     bool _is_left_eye    = false;
-    int _eyelid_offset_y = 0;
+    void updateGeometry();
 
     std::unique_ptr<uitk::lvgl_cpp::Container> _container;
     std::unique_ptr<uitk::lvgl_cpp::Container> _eye;
+    std::unique_ptr<uitk::lvgl_cpp::Container> _iris;
+    std::unique_ptr<uitk::lvgl_cpp::Container> _pupil;
+    std::unique_ptr<uitk::lvgl_cpp::Container> _highlight;
     std::unique_ptr<uitk::lvgl_cpp::Container> _eyelid;
+    std::unique_ptr<uitk::lvgl_cpp::Container> _lower_lid;
 };
 
 /**
diff --git a/firmware/main/stackchan/avatar/skins/default/eyes.cpp b/firmware/main/stackchan/avatar/skins/default/eyes.cpp
index 32c6c1f..a2cb84e 100644
--- a/firmware/main/stackchan/avatar/skins/default/eyes.cpp
+++ b/firmware/main/stackchan/avatar/skins/default/eyes.cpp
@@ -1,48 +1,43 @@
 /*
  * SPDX-FileCopyrightText: 2026 M5Stack Technology CO LTD
- *
  * SPDX-License-Identifier: MIT
+ * Stakia eye treatment: layered sclera, iris, pupil, highlight and two lids.
  */
 #include "default.h"
+#include "stakia_geometry.h"
 
 using namespace uitk;
 using namespace uitk::lvgl_cpp;
 using namespace stackchan::avatar;
 
-static const int _eye_size            = 16;
-static const Vector2i _eye_pos        = Vector2i(-70, -16);
-static const Vector2i _eye_min_offset = Vector2i(-16, -16);
-static const Vector2i _eye_max_offset = Vector2i(16, 16);
-static const Vector2i _eye_size_limit = Vector2i(8, 32);
-
 DefaultEyes::DefaultEyes(lv_obj_t* parent, lv_color_t primaryColor, lv_color_t secondaryColor, bool isLeftEye)
 {
     _is_left_eye = isLeftEye;
-
-    _container = std::make_unique<Container>(parent);
-    _container->setRadius(0);
-    _container->setAlign(LV_ALIGN_CENTER);
-    _container->setBorderWidth(0);
+    auto part = [](lv_obj_t* p, lv_color_t color, int radius) {
+        auto obj = std::make_unique<Container>(p);
+        obj->setAlign(LV_ALIGN_CENTER);
+        obj->setBorderWidth(0);
+        obj->setPadding(0, 0, 0, 0);
+        obj->setRadius(radius);
+        obj->setBgColor(color);
+        obj->removeFlag(LV_OBJ_FLAG_SCROLLABLE);
+        // Inherit neither theme shadows nor clickable hit targets.
+        lv_obj_set_style_shadow_width(obj->get(), 0, 0);
+        lv_obj_remove_flag(obj->get(), LV_OBJ_FLAG_CLICKABLE);
+        return obj;
+    };
+    _container = part(parent, secondaryColor, 0);
     _container->setBgOpa(0);
-    _container->removeFlag(LV_OBJ_FLAG_SCROLLABLE);
-    _container->setPadding(0, 0, 0, 0);
-    _container->setTransformPivot(_eye_size_limit.y / 2, _eye_size_limit.y / 2);
-    _container->setSize(_eye_size_limit.y, _eye_size_limit.y);
-
-    _eye = std::make_unique<Container>(_container->get());
-    _eye->setRadius(LV_RADIUS_CIRCLE);
-    _eye->align(LV_ALIGN_CENTER, 0, 0);
-    _eye->setBorderWidth(0);
-    _eye->setBgColor(primaryColor);
-    _eye->removeFlag(LV_OBJ_FLAG_SCROLLABLE);
-
-    _eyelid = std::make_unique<Container>(_container->get());
-    _eyelid->setRadius(0);
-    _eyelid->align(LV_ALIGN_CENTER, 0, 0);
-    _eyelid->setBorderWidth(0);
-    _eyelid->setBgColor(secondaryColor);
-    _eyelid->removeFlag(LV_OBJ_FLAG_SCROLLABLE);
-
+    _container->setSize(116, 128);
+    _container->setTransformPivot(58, 64);
+    _eye = part(_container->get(), primaryColor, 38);
+    _iris = part(_eye->get(), lv_color_hex(0x27CFA8), LV_RADIUS_CIRCLE);
+    _pupil = part(_iris->get(), lv_color_hex(0x071A20), LV_RADIUS_CIRCLE);
+    _highlight = part(_iris->get(), lv_color_white(), LV_RADIUS_CIRCLE);
+    _highlight->setSize(10, 10);
+    _highlight->setPos(-12, -13);
+    _eyelid = part(_container->get(), secondaryColor, 0);
+    _lower_lid = part(_container->get(), secondaryColor, 0);
     setSize(0);
     setWeight(100);
     setPosition(_position);
@@ -51,94 +46,73 @@ DefaultEyes::DefaultEyes(lv_obj_t* parent, lv_color_t primaryColor, lv_color_t s
 
 DefaultEyes::~DefaultEyes()
 {
+    _lower_lid.reset();
     _eyelid.reset();
+    _highlight.reset();
+    _pupil.reset();
+    _iris.reset();
     _eye.reset();
     _container.reset();
 }
 
+void DefaultEyes::updateGeometry()
+{
+    const auto g = stakia::geometry(_size, _weight, _position.x, _position.y);
+    _eye->setSize(g.width, g.height);
+    _iris->setSize(g.iris, g.iris);
+    _iris->setPos(g.gaze_x, g.gaze_y);
+    _pupil->setSize(g.pupil, g.pupil);
+    // Top-left alignment avoids rounding gaps during a complete blink.
+    _eyelid->setAlign(LV_ALIGN_TOP_LEFT);
+    _lower_lid->setAlign(LV_ALIGN_TOP_LEFT);
+    _eyelid->setSize(116, g.top_cover);
+    _eyelid->setPos(0, (128 - g.height) / 2);
+    _lower_lid->setSize(116, g.bottom_cover);
+    _lower_lid->setPos(0, (128 + g.height) / 2 - g.bottom_cover);
+    _eyelid->setHidden(g.top_cover == 0);
+    _lower_lid->setHidden(g.bottom_cover == 0);
+}
+
 void DefaultEyes::setPosition(const Vector2i& position)
 {
     Element::setPosition(position);
-
-    auto pos_x = _is_left_eye ? _eye_pos.x : -_eye_pos.x;
-    pos_x += map_range(_position.x, -100, 100, _eye_min_offset.x, _eye_max_offset.x);
-    auto pos_y = _eye_pos.y + map_range(_position.y, -100, 100, _eye_min_offset.y, _eye_max_offset.y);
-
-    _container->setPos(pos_x, pos_y);
-    _eyelid->setY(_eyelid_offset_y);
+    // Gaze moves the iris within a stable face, rather than sliding both eyes.
+    _container->setPos(_is_left_eye ? -74 : 74, -22);
+    updateGeometry();
 }
-
 void DefaultEyes::setWeight(int weight)
 {
     Feature::setWeight(weight);
-
-    _eyelid_offset_y = -map_range(_weight, 0, 100, 0, (int)_eyelid->getHeight());
-
-    _eyelid->setY(_eyelid_offset_y);
+    updateGeometry();
 }
-
-void DefaultEyes::setRotation(int rotation)
+void DefaultEyes::setSize(int size)
 {
-    Element::setRotation(rotation);
-
-    _container->setRotation(rotation);
+    Feature::setSize(size);
+    updateGeometry();
 }
-
-void DefaultEyes::setEmotion(const Emotion& emotion)
+void DefaultEyes::setRotation(int rotation)
 {
-    if (getIgnoreEmotion()) {
-        return;
-    }
-
-    auto apply_style = [this](int weight, int rotation) {
-        setWeight(weight);
-        if (_is_left_eye) {
-            setRotation(rotation);
-        } else {
-            setRotation(-rotation);
-        }
-    };
-
-    switch (emotion) {
-        case Emotion::Neutral:
-            apply_style(100, 0);
-            break;
-        case Emotion::Happy:
-            apply_style(72, 1550);
-            break;
-        case Emotion::Angry:
-            apply_style(70, 450);
-            break;
-        case Emotion::Sad:
-            apply_style(70, -400);
-            break;
-        case Emotion::Doubt:
-            apply_style(75, 0);
-            break;
-        case Emotion::Sleepy:
-            apply_style(35, -50);
-            break;
-        default:
-            break;
-    }
+    Element::setRotation(stakia::wrap_rotation(rotation));
+    _container->setRotation(_rotation);
 }
-
 void DefaultEyes::setVisible(bool visible)
 {
     Element::setVisible(visible);
-
     _container->setHidden(!visible);
 }
-
-void DefaultEyes::setSize(int size)
+void DefaultEyes::setEmotion(const Emotion& emotion)
 {
-    Feature::setSize(size);
-
-    int eye_size = map_range(_size, -100, 100, _eye_size_limit.x, _eye_size_limit.y);
-
-    _eye->setSize(eye_size, eye_size);
-    _eyelid->setSize(eye_size, eye_size);
-
-    // Force eyelid update
-    setWeight(getWeight());
+    if (getIgnoreEmotion()) return;
+    int weight = 100;
+    int rotation = 0;
+    switch (emotion) {
+        case Emotion::Happy: weight = 74; rotation = -80; break;
+        case Emotion::Angry: weight = 60; rotation = 120; break;
+        case Emotion::Sad: weight = 68; rotation = -100; break;
+        case Emotion::Doubt: weight = _is_left_eye ? 54 : 88; rotation = -50; break;
+        case Emotion::Sleepy: weight = 28; rotation = -30; break;
+        default: break;
+    }
+    setWeight(weight);
+    setRotation(_is_left_eye ? rotation : -rotation);
 }
diff --git a/firmware/main/stackchan/avatar/skins/default/mouth.cpp b/firmware/main/stackchan/avatar/skins/default/mouth.cpp
index 02b683c..0c3ead9 100644
--- a/firmware/main/stackchan/avatar/skins/default/mouth.cpp
+++ b/firmware/main/stackchan/avatar/skins/default/mouth.cpp
@@ -9,11 +9,11 @@ using namespace uitk;
 using namespace uitk::lvgl_cpp;
 using namespace stackchan::avatar;
 
-static const Vector2i _mouth_pos        = Vector2i(0, 26);
+static const Vector2i _mouth_pos        = Vector2i(0, 67);
 static const Vector2i _mouth_min_offset = Vector2i(-16, -16);
 static const Vector2i _mouth_max_offset = Vector2i(16, 16);
-static const Vector2i _mouth_min_size   = Vector2i(90, 6);
-static const Vector2i _mouth_max_size   = Vector2i(60, 50);
+static const Vector2i _mouth_min_size   = Vector2i(48, 5);
+static const Vector2i _mouth_max_size   = Vector2i(36, 28);
 static const int _mouth_min_radius      = 0;
 static const int _mouth_max_radius      = 16;
 
diff --git a/firmware/main/stackchan/avatar/skins/default/stakia_geometry.h b/firmware/main/stackchan/avatar/skins/default/stakia_geometry.h
new file mode 100644
index 0000000..ee42cd3
--- /dev/null
+++ b/firmware/main/stackchan/avatar/skins/default/stakia_geometry.h
@@ -0,0 +1,20 @@
+// SPDX-License-Identifier: MIT
+#pragma once
+namespace stakia {
+constexpr int clamp(int x, int lo, int hi) { return x < lo ? lo : (x > hi ? hi : x); }
+constexpr int wrap_rotation(int angle) { return ((angle % 3600) + 3600) % 3600; }
+struct EyeGeometry {
+    int width, height, iris, pupil, gaze_x, gaze_y, top_cover, bottom_cover;
+};
+constexpr EyeGeometry geometry(int size, int openness, int gaze_x, int gaze_y) {
+    const int s = clamp(size, -100, 100);
+    const int w = 96 + s * 8 / 100;
+    const int h = 108 + s * 8 / 100;
+    const int iris = 54 + s * 4 / 100;
+    const int closed = h * (100 - clamp(openness, 0, 100)) / 100;
+    return {w, h, iris, iris * 48 / 100,
+            clamp(gaze_x, -100, 100) * 12 / 100,
+            clamp(gaze_y, -100, 100) * 14 / 100,
+            (closed + 1) / 2, closed / 2};
+}
+}
