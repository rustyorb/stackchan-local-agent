# Stakia firmware

Use [STAKIA-BUILD.md](STAKIA-BUILD.md) for the current local-server firmware and expressive eyes.

The older `build.bat`, `flash.bat`, and `sdkconfig.override` are historical generic-CoreS3 tooling from April. They are not the build path for this branch.
